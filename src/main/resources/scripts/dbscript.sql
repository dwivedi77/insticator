CREATE TABLE Employee
(
tid integer,
name varchar(255),
sex varchar(1),
age integer,
employee_type integer,
salary float(10)
);

CREATE TABLE Employee_Type
(
typeid integer,
type varchar(10),
rate_type varchar(10),
);